apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: batch
resources:
  - ../../base

configMapGenerator:
  - name: batch-html
    files:
      - index.html=html/index.html

patchesStrategicMerge:
  - deployment.yaml
