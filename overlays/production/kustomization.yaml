apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: batch
resources:
  - ../../base
  - canary.yaml
  - helmrelease_postgres.yaml
  - hpa.yaml
  - secret_batch-postgres.enc.yaml


configMapGenerator:
  - name: batch-html
    files:
      - index.html=html/index.html
