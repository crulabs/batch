apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: batch
  namespace: batch
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: batch
  minReplicas: 1
  maxReplicas: 1